# TDD

Before implementing non-trivial behavior, read `.agents/skills/tdd/`.
Write the smallest failing test first, run it, and confirm it fails for the expected reason before production code.
If TDD is being skipped because the change is mechanical or the harness is missing, say that explicitly.
