import * as React from "react";
import { DocsContent } from "./docs-content";

export const metadata = {
  title: "Documentation | DBX Agent UI",
};

export default function DocsPage() {
  return <DocsContent />;
}
